import express from 'express';
// import { savePost,fetchPosts,editpost,deletePost,allPosts,postDetails, likePost} from '../controllers/postsController';
// import {uploadPostImages} from '../services/awsconfig'

const router = express.Router();
// router.post('/savepost',uploadPostImages.array('images',10),savePost)
// router.patch('/editpost',uploadPostImages.array('images',10),editpost)
// router.get('/post',fetchPosts)
// router.get('/allposts',allPosts)
// router.get('/postdetails/:post_id',postDetails)
// router.delete('/deletepost/:post_id',deletePost)
// router.post('/like/:post_id',likePost)

export default router;